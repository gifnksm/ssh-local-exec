services:
  server:
    hostname: server
    build:
      context: ../../
      dockerfile: tests/docker/Dockerfile
    image: ssh-local-exec-test:${TAG:-latest}
    privileged: true
  client:
    hostname: client
    image: ssh-local-exec-test:${TAG:-latest}
    privileged: true
